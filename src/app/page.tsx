import Hero from "@/components/Hero";
import Nav from "@/components/Nav";

const Homepage = () => {
  return (
      <div>
        <nav>
          <Nav />
          <Hero />
        </nav>
      </div>
  );
};

export default Homepage;
