import Hero from "@/components/Hero";
import Nav from "@/components/Nav";

const Homepage = () => {
  return (
      <div>
          <Nav />
          <Hero />
      </div>
  );
};

export default Homepage;
